This spring boot application exposes 2 rest end points api/addEmployee and api/fetchEmployee
to insert employee data into NoSql mongodb database and retrieve them back based on id.
It uses MongoRepository for CRUD operations on mongodb.
